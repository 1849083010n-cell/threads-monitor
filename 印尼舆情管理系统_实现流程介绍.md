# 印尼舆情管理系统 — 实现流程介绍

> **系统定位**：基于 Meta Threads API 的印尼市场舆情监控平台
> **监控对象**：品牌关键词（如 Chery）在 Threads 上的公开讨论
> **核心能力**：OAuth 授权绑定、定时舆情抓取、印尼语情感分析、告警推送

---

## 一、系统概述

### 1.1 建设目标

| 目标 | 说明 |
|------|------|
| 舆情采集 | 通过 Meta Threads API 定时拉取品牌相关帖子 |
| 情感研判 | 对帖子内容进行印尼语情感分析，归类为正/负/中性 |
| 风险预警 | 负面舆情达到阈值时自动推送告警 |
| 运营支撑 | 提供运营后台可视化查看舆情动态 |

### 1.2 技术选型

| 层级 | Demo 阶段 | 生产阶段 |
|------|-----------|----------|
| 前端 | Flask 内置模板（单页） | 独立运营后台（React/Vue） |
| 后端 | Python + Flask | Node.js / Python（FastAPI） |
| 数据库 | SQLite（tokens.db） | PostgreSQL / MySQL |
| 任务调度 | 手动触发搜索 | APScheduler / Cron 定时任务 |
| 情感分析 | 英文词典规则 | 印尼语 NLP（词典 / API / 自建模型） |

---

## 二、系统架构（对应架构分析图）

```
┌─────────────────────────────────────────────────────────────┐
│                       前端 / UI 层                          │
│                    （运营后台 Web 页面）                      │
│        - 授权状态展示（已授权/未授权）                        │
│        - 关键词搜索入口 + 搜索结果列表（含情感标签）           │
└──────────────────────┬──────────────────────────────────────┘
                       │ HTTP / JSON
┌──────────────────────▼──────────────────────────────────────┐
│                    后端服务层                                │
│  ┌─────────────────┐   ┌───────────────────────────────┐    │
│  │  授权控制器      │   │  定时任务调度器                │    │
│  │  (OAuth)        │   │  (Cron / Scheduler)           │    │
│  │  - 生成授权URL  │   │  - 每30分钟拉取一次            │    │
│  │  - 接收回调code │   │  - 关键词: Chery              │    │
│  │  - 兑码换Token  │   └──────────────┬────────────────┘    │
│  └────────┬────────┘                  │                     │
│           ▼                           ▼                     │
│  ┌──────────────────────────────────────────────┐           │
│  │              业务逻辑层                       │           │
│  │  - 印尼语 NLP 情感分析                       │           │
│  │  - 舆情归类（正面/负面/中性）                │           │
│  │  - 告警阈值判断                              │           │
│  └──────────────────────┬───────────────────────┘           │
│                         ▼                                   │
│  ┌──────────────────────────────────────────────┐           │
│  │              数据访问层 (DAO)                 │           │
│  │  - Token 加解密读写                          │           │
│  │  - 舆情数据存储                              │           │
│  └──────────────────────┬───────────────────────┘           │
└─────────────────────────┼───────────────────────────────────┘
                          ▼
┌──────────────────────────────────────────────┐
│              数据库层                         │
│  ┌────────────┐    ┌─────────────────────┐   │
│  │ token表    │    │ 舆情帖子表           │   │
│  │ user_id    │    │ thread_id           │   │
│  │ token      │    │ content (印尼语)    │   │
│  │ 过期时间   │    │ sentiment_score     │   │
│  │ 权限状态   │    │ 发布时间            │   │
│  └────────────┘    │ 关键词匹配          │   │
│                    └─────────────────────┘   │
└─────────────────────┬────────────────────────┘
                      ▼
┌──────────────────────────────────────────────┐
│          外部依赖 / 第三方服务                │
│  ① Meta Threads API (graph.threads.net)     │
│  ② 印尼语分词 / 情感 API（或自建模型）       │
│  ③ 告警推送 (Telegram / Email / Webhook)    │
└──────────────────────────────────────────────┘
```

### 2.1 各层职责说明

1. **前端/UI 层**：运营人员操作入口。展示授权状态、搜索框、搜索结果（帖子内容 + 情感标签 + 情感分数）。
2. **授权控制器**：OAuth 全流程处理——生成授权 URL、接收 Meta 回调 code、兑换短期 Token、换取长期 Token。
3. **定时任务调度器**：生产环境核心。每 30 分钟自动拉取关键词帖子，无需人工干预。
4. **业务逻辑层**：情感分析（印尼语）、舆情归类、告警阈值判断三大核心算法。
5. **数据访问层 (DAO)**：Token 加密存储与读取、舆情帖子的写入与查询。
6. **数据库层**：token 表（凭证）与舆情帖子表（内容 + 情感分 + 时间 + 关键词）。
7. **外部依赖**：Threads API（数据源）、印尼语 NLP（分析能力）、告警通道（通知出口）。

---

## 三、实施流程（分阶段执行）

### 阶段一：Meta 开发者后台前置准备（手动，约 15 分钟）

> 此阶段无法代码自动化，必须人工完成。

| 步骤 | 操作 | 要点 |
|------|------|------|
| 1 | 登录 [Meta for Developers](https://developers.facebook.com/apps/) | 注册/登录开发者账号 |
| 2 | 创建新应用 | **Use Case 必须选 "Access the Threads API"** |
| 3 | 进入 Use Cases → Access the Threads API → Customize | 记录 Threads App ID / App Secret（均以 `THREADS_` 开头） |
| 4 | 配置 Redirect Callback URLs | 添加 `http://localhost:5000/callback` |
| 5 | App Roles → Roles → Add People | 添加 **Threads Tester** 角色，填入自己的 Threads 用户名 |
| 6 | 登录 Threads 账号 → 设置 → 网站权限 → 邀请 | **接受测试邀请（必须做，否则授权失败）** |

### 阶段二：项目初始化（约 5 分钟）

```bash
# 1. 创建项目文件夹
mkdir threads-舆情监控 && cd threads-舆情监控

# 2. 创建 Python 虚拟环境
python -m venv venv

# 3. 激活虚拟环境（Mac/Linux）
source venv/bin/activate

# 4. 安装依赖库
pip install flask requests python-dotenv
```

### 阶段三：配置环境变量（.env）

创建 `.env` 文件，填入阶段一拿到的真实凭证：

```env
THREADS_APP_ID=你的Threads_App_ID
THREADS_APP_SECRET=你的Threads_App_Secret
THREADS_REDIRECT_URI=http://localhost:5000/callback
```

### 阶段四：核心代码开发（app.py）

单文件包含全部前后端逻辑，模块划分如下：

| 模块 | 功能 |
|------|------|
| 数据库初始化 | SQLite 建表（token 表） |
| Token 存取 | `save_token()` / `get_token()` / `is_token_valid()` |
| 页面模板 | HTML + CSS + JS 前端（授权按钮、搜索框、结果列表） |
| `/auth` 路由 | 生成授权 URL 并跳转 Meta |
| `/callback` 路由 | 收 code → 兑短期 Token → 换长期 Token → 存库 |
| `/logout` 路由 | 清除 Token |
| `/search` 路由 | 调 Threads 搜索 API → 取详情 → 情感分析 → 返回结果 |
| 情感分析模块 | 词典规则（正面词 / 负面词 / 分数计算） |

### 阶段五：运行与验收（约 10 分钟）

```bash
python app.py
# 访问 http://localhost:5000
```

**验收清单：**
- [ ] 页面初始状态显示「❌ 未授权」
- [ ] 点击「🔑 授权 Threads 账号」跳转 Meta 授权页
- [ ] 登录测试账号并同意权限（threads_basic / threads_keyword_search / threads_read_replies）
- [ ] 授权成功跳回首页，状态变「✅ 已授权」
- [ ] 项目根目录生成 tokens.db
- [ ] 输入关键词（如 Chery）→ 点击搜索 → 返回帖子列表
- [ ] 每条帖子带情感标签（正面 😊 / 负面 😞 / 中性 😐）和分数
- [ ] 页面无报错

---

## 四、核心业务流程（对应时序图）

### 流程 1：OAuth 账号授权

```
运营人员      前端(UI)        后端服务         Meta API          数据库
   │            │              │                │                │
   │ 点击"授权  │              │                │                │
   │ Threads"   │              │                │                │
   ├───────────>│              │                │                │
   │            │ 请求授权URL  │                │                │
   │            ├─────────────>│                │                │
   │            │              │ 生成带参数的   │                │
   │            │              │ 授权URL        │                │
   │            │<─────────────│                │                │
   │            │ 跳转Meta授权页│                │                │
   │  <───────────────跳转──────────────────>   │                │
   │ 登录账号、勾选权限、确认                    │                │
   │            │ 回调 redirect_uri?code=xxx    │                │
   │            ├─────────────>│                │                │
   │            │              │ POST /oauth/   │                │
   │            │              │ token          │                │
   │            │              │ (id+secret+code)                │
   │            │              ├───────────────>│                │
   │            │              │ 返回短期Token  │                │
   │            │              │<───────────────│                │
   │            │              │ GET /oauth/    │                │
   │            │              │ token 换长期   │                │
   │            │              ├───────────────>│                │
   │            │              │ 返回长期Token  │                │
   │            │              │<───────────────│                │
   │            │              │ 加密存储Token  │                │
   │            │              ├────────────────────────────────>│
   │            │ 授权成功页面  │                │                │
   │<───────────│<─────────────│                │                │
```

### 流程 2：定时舆情抓取（生产核心）

```
后端调度器      数据库          Meta API        业务逻辑层      告警通道
   │              │              │                │              │
   │ [Cron触发]   │              │                │              │
   │ 每30分钟      │              │                │              │
   │ 读取Token     │              │                │              │
   ├─────────────>│              │                │              │
   │              │ 返回Token    │                │              │
   │<─────────────│              │                │              │
   │              │ 调用搜索API  │                │              │
   │              │ /threads/    │                │              │
   │              │ search?q=Chery                │              │
   │              ├─────────────>│                │              │
   │              │ 返回帖子列表 │                │              │
   │              │<─────────────│                │              │
   │              │ 情感分析     │                │              │
   │              │ (印尼语NLP)  │                │              │
   │              │ 舆情归类     │                │              │
   │              ├──────────────┼───────────────>│              │
   │              │ 存储帖子     │                │              │
   │              ├─────────────────────────────────────────────>│
   │              │ 检查告警阈值 │                │              │
   │              │ (负面分<阈值)│                │              │
   │              ├──────────────┼───────────────>│              │
   │              │              │                │ 推送告警     │
   │              │              │                ├─────────────>│
   │              │              │                │ (Telegram/   │
   │              │              │                │  Email/      │
   │              │              │                │  Webhook)    │
```

### 流程 3：手动搜索 + 实时分析（Demo 核心）

1. 用户输入英文关键词（如 Chery）→ 点击搜索
2. 前端 `fetch('/search')` 发送 POST 请求
3. 后端校验 Token 有效性（过期返回 401）
4. 调用 `graph.threads.net/v1.0/threads_search?query=关键词`
5. 遍历帖子 ID，逐个请求详情接口获取 text / username / timestamp
6. 对每条 text 执行情感分析，计算分数并归类
7. 返回帖子列表 + 情感标签 → 前端渲染展示

---

## 五、数据模型设计

### 5.1 token 表（凭证存储）

| 字段 | 类型 | 说明 |
|------|------|------|
| id | INTEGER PK | 主键 |
| access_token | TEXT | 长期访问令牌（生产环境需加密） |
| user_id | TEXT | Threads 用户 ID |
| expires_at | TEXT | 过期时间（长期 Token 约 60 天） |

### 5.2 舆情帖子表（生产扩展）

| 字段 | 类型 | 说明 |
|------|------|------|
| thread_id | TEXT PK | Threads 帖子 ID |
| content | TEXT | 帖子内容（印尼语） |
| sentiment_score | FLOAT | 情感分数（-1 ~ 1） |
| sentiment_label | TEXT | 情感标签（正/负/中性） |
| published_at | DATETIME | 发布时间 |
| keyword | TEXT | 命中的监控关键词 |

---

## 六、生产化增强路线（MD 文件第六部分延伸）

### 6.1 当前 Demo 的限制

| 限制 | 说明 |
|------|------|
| API 配额 | threads_keyword_search 每 7 天滚动仅 500 次查询 |
| Token 刷新 | 长期 Token 60 天过期，Demo 无自动刷新 |
| 语言支持 | 仅英文词典，印尼语需替换词典或接 NLP API |
| 数据持久化 | 搜索结果未入库，仅实时展示 |

### 6.2 升级路线图

| 优先级 | 增强项 | 实施方案 |
|--------|--------|----------|
| P0 | 印尼语情感分析 | 替换为印尼语情感词典（bagus/jelek 等）或接入印尼语 NLP API / 自建模型 |
| P0 | 定时任务 | 集成 APScheduler，每 30 分钟自动拉取指定关键词 |
| P0 | 告警推送 | 负面分数低于阈值时自动发 Telegram / 邮件 / Webhook |
| P1 | 自动刷新 Token | 请求前检查有效期，剩余 <7 天自动调用刷新接口并更新库 |
| P1 | 数据入库 | 搜索结果写入舆情帖子表，支持历史趋势分析 |
| P2 | 运营后台 | 独立前端，增加舆情看板、趋势图、日报周报导出 |

---

## 七、常见问题排查速查表

| 现象 | 排查方向 |
|------|----------|
| 授权后跳转 localhost 被拒绝 | 检查 Meta 后台 Redirect URI 是否精确为 `http://localhost:5000/callback`（端口+路径） |
| 搜索返回空结果 | 关键词是否为英文；Threads 上是否真的有人讨论该词 |
| 搜索报 403 / 权限不足 | 授权时是否勾选 threads_keyword_search；**测试账号是否已接受邀请（最易忽略）** |
| 模块导入错误 | 是否激活虚拟环境；是否安装 flask / requests / python-dotenv |

---

## 八、整体实施时间线

| 阶段 | 耗时 | 产出 |
|------|------|------|
| 一、Meta 后台配置 | 15 分钟 | App ID / Secret / 回调 / 测试账号就绪 |
| 二、项目初始化 | 5 分钟 | 虚拟环境 + 依赖 |
| 三、环境变量 | 2 分钟 | .env 配置完成 |
| 四、代码开发 | 30 分钟 | app.py 可运行 |
| 五、运行验收 | 10 分钟 | 授权 ✓ 搜索 ✓ 情感分析 ✓ |
| 六、生产增强 | 1~2 天 | 定时抓取 + 印尼语 + 告警 + 数据入库 |

**总计：Demo 半天可跑通，生产版 1~2 天完成核心增强。**
