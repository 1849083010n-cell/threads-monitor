# Threads 舆情监控 Demo — 当前进度记录

> 更新日期：2026-08-23
> 状态：代码 ✅ 已跑通 | 本地服务 ✅ 运行中 | Meta 授权 ⏸ 卡在后台表单保存

---

## 一、当前可用状态

| 项目 | 状态 | 说明 |
|------|------|------|
| 本地服务 | ✅ 运行中 | http://localhost:5001（Flask + SQLite） |
| 公网隧道 | ✅ 运行中 | https://snub-cataract-launder.ngrok-free.dev（静态域名，重启不变） |
| 情感分析模块 | ✅ 验证通过 | 正面/负面/中性 正常 |
| OAuth 授权 | ⏸ 卡住 | Meta 后台 Threads 表单无法保存，见下 |

## 二、卡点：Meta 后台「無法儲存表格」

### 现象
- 位置：Meta 开发者后台 → 使用案例 → Access Threads API → Set up
- 字段：「重新導向回調網址」（Redirect the return URL）
- 无论填什么 URL 都保存失败：
  - https://snub-cataract-launder.ngrok-free.dev/callback ❌
  - https://snub-cataract-launder.ngrok-free.dev ❌
  - https://www.google.com/callback ❌
- 报错：紅色橫幅「無法儲存表格 / 請確認你輸入的所有資訊皆正確無誤」

### 已排除的原因
- ❌ URL 格式（全半角、空格、大小写都检查过，没问题）
- ❌ URL 内容（google.com 也失败，不是地址问题）
- ❌ 输入法全角字符（已切换英文输入法）

### 最可能的原因（待验证）
- ⚠️ **VPN 出口 IP 被 Meta 拦截**：保存操作走 API，VPN IP 被标记时页面能打开但保存请求被拒
  - 待试：关闭 VPN / 换节点 / 换网络（手机热点）/ 无痕窗口 / 换浏览器

### 表单当前显示值（最后一次截图确认）
- Threads 顯示名稱：舆情管理chery
- 重新導向回調網址：https://snub-cataract-launder.ngrok-free.dev（显示值，但未保存成功）
- Threads Application Number：1961044887918102
- Threads application key：掩码（需点 Display 核对是否 = 87d50f80a867c44a47b2ac12dfa28be2）

## 三、已解决的坑（记录备查）

| 错误 | 原因 | 解决 |
|------|------|------|
| 4476002 未发送应用编号 | .env 填了通用 App ID，不是 Threads App ID | 换为 Threads 专用 ID 1961044887918102 |
| 1349187 已阻止不安全登录 | 回调是 http://localhost | 改用 HTTPS（ngrok 地址） |
| 1349168 跳转 URI 未入白名单 | Threads 用例 Redirect URL 还是 localhost:5001 | 需把 ngrok 地址保存进表单（卡点） |
| 5000 端口被占用 | macOS AirPlay Receiver | 改用 5001 端口 |
| 授权 URL 域名过时 | 旧文档用 threads.net | 官方现为 https://threads.com/oauth/authorize |
| scope 过时 | 旧文档含 threads_keyword_search | 官方 scope 只含 threads_basic,threads_read_replies 等 |

## 四、关键配置现状

### .env（保持不变，勿动）
```
THREADS_APP_ID=1961044887918102
THREADS_APP_SECRET=87d50f80a867c44a47b2ac12dfa28be2
THREADS_REDIRECT_URI=https://snub-cataract-launder.ngrok-free.dev/callback
```

### 代码关键点（app.py）
- 授权 URL：https://threads.com/oauth/authorize
- scope：threads_basic,threads_read_replies
- 端口：5001
- Threads App ID / Secret 位置：Meta 后台 → 使用案例 → Access Threads API → Set up

## 五、下一步行动（按顺序）

1. **解决表单保存**：关 VPN / 换节点 / 换网络 / 无痕窗口，重试保存
   - 成功标准：Redirect the return URL 能保存，页面刷新后值还在
2. **保存正确的回调地址**：https://snub-cataract-launder.ngrok-free.dev/callback
   （若 ngrok 域名被 Meta 拒绝，备选：mkcert 本地 HTTPS https://localhost:5001）
3. **重新授权**：http://localhost:5001 → 授权 → 登录 → 同意
4. **验证**：tokens.db 写入 token、页面显示「✅ 已授权」
5. **搜索测试**：测试账号先发 2-3 条含关键词的帖子（如 "I love Chery car"），再搜 "Chery"

## 六、日常命令

```bash
# 启动服务
cd ~/Desktop/印尼舆情管理/threads-舆情监控 && source venv/bin/activate && python app.py

# 启动 ngrok（如未运行）
ngrok http 5001

# 验证授权是否成功（看 token）
cd ~/Desktop/印尼舆情管理/threads-舆情监控 && source venv/bin/activate && python3 -c "import sqlite3; c=sqlite3.connect('tokens.db').cursor(); print(c.execute('SELECT user_id, expires_at FROM token').fetchall())"
```
